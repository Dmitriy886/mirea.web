let st = Math.pow(2, 10);

alert(st);
  
let a = Math.sqrt(245, 2);

alert(a);

let arr = [4, 2, 5, 19, 13, 0, 10], answer = 0;

for (let i = 0; i < arr.length; i++)
    answer += Math.pow(arr[i], 3);

alert(Math.sqrt(answer, 2));